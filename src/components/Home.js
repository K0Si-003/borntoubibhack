import React from "react";
import Searchbar from "../components/Searchbar";

const Home = () => {
    return (
        <main>
            <Searchbar />

        </main>
    );
};

export default Home;
